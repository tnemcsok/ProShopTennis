import Link from 'next/link';
import NextCompetitions from '../components/NextCompetitions'
import PreviousCompetitions from '../components/PreviousCompetitions'


const Versenynaptár = props => (
  <div>
    <NextCompetitions />
    <PreviousCompetitions />
  </div>
);

export default Versenynaptár;
